<h1><%= @playlist.name %></h1>
<p><%= @playlist.description %></p>

<ul>
  <% @playlist_tracks.each do |track| %>
    <li>
      <%= link_to track.title, track_path(track.sc_id) %> <!-- later on: somehow mark on track view if it's already in this playlist -->
      <%= link_to "Remove from Playlist", user_playlist_track_id_path(@user, @playlist, track.track_id), method: :delete, data: { confirm: "Are You Sure?"} %>
    </li>
  <% end %>
</ul>

<%= link_to "Delete Playlist", user_playlist_path(@user, @playlist), method: :delete, data: { confirm: "Are You Sure?"} %>
